---
phase: 09-data-layer
plan: 01
type: execute
wave: 1
depends_on: ["08-02"]
files_modified:
  - sanity/lib/client.ts
  - sanity/lib/fetch.ts
  - sanity/lib/queries.ts
  - sanity/lib/live.ts
  - app/api/revalidate/route.ts
  - app/api/draft/enable/route.ts
  - app/api/draft/disable/route.ts
  - .env.local
autonomous: true
---

<objective>
## Goal
Créer la couche data Next.js : sanityFetch avec support draft mode, queries GROQ portées, et les 3 routes API (revalidation ISR + draft mode).

## Purpose
Poser l'infrastructure server-side qui permettra en 09-02 de convertir les pages en Server Components, et en phase 10 d'activer le live preview.

## Output
- sanity/lib/client.ts — client Sanity pour Next.js (token read)
- sanity/lib/fetch.ts — sanityFetch avec cache + draft mode
- sanity/lib/queries.ts — toutes les queries GROQ (portées depuis src/lib/sanityQueries.ts)
- sanity/lib/live.ts — configuration live queries
- app/api/revalidate/route.ts — webhook Sanity → revalidation ISR
- app/api/draft/enable/route.ts — active draft mode (cookie)
- app/api/draft/disable/route.ts — désactive draft mode
- .env.local mis à jour avec les nouvelles variables
</objective>

<context>
@.paul/PROJECT.md
@src/lib/sanityQueries.ts
@src/lib/sanityClient.ts

## Variables d'env nécessaires
- NEXT_PUBLIC_SANITY_PROJECT_ID=z0uyxhwg
- NEXT_PUBLIC_SANITY_DATASET=production
- SANITY_API_READ_TOKEN — token Sanity avec permission "Viewer" (pour draft mode)
- SANITY_REVALIDATE_SECRET — secret aléatoire pour sécuriser le webhook

## Dépendances à installer
- next-sanity (fournit sanityFetch, SanityLive, defineEnableDraftMode)
</context>

<acceptance_criteria>

## AC-1: sanityFetch retourne les données Sanity
```gherkin
Given sanity/lib/fetch.ts configuré avec le project ID
When on appelle sanityFetch avec une query GROQ valide
Then les données Sanity sont retournées sans erreur
```

## AC-2: Route revalidate sécurisée
```gherkin
Given app/api/revalidate/route.ts avec SANITY_REVALIDATE_SECRET
When Sanity envoie un POST avec le bon secret en Authorization header
Then la route retourne 200 et déclenche revalidateTag('sanity')
```

## AC-3: Draft mode activable
```gherkin
Given app/api/draft/enable/route.ts
When on visite /api/draft/enable?secret=SANITY_REVALIDATE_SECRET
Then le cookie __prerender_bypass est posé et on est redirigé vers /
```

## AC-4: Draft mode désactivable
```gherkin
Given draft mode actif
When on visite /api/draft/disable
Then le cookie draft mode est supprimé et on est redirigé vers /
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Installer next-sanity</name>
  <files>package.json</files>
  <action>
    ```bash
    npm install next-sanity
    ```
    next-sanity fournit : sanityFetch, SanityLive, defineEnableDraftMode, NextStudio.
    Si next-sanity était déjà installé en 08-02 (pour NextStudio), cette étape est déjà faite.
  </action>
  <verify>grep '"next-sanity"' package.json</verify>
  <done>Dépendance présente</done>
</task>

<task type="auto">
  <name>Task 2: Créer sanity/lib/client.ts</name>
  <files>sanity/lib/client.ts</files>
  <action>
    ```ts
    import { createClient } from 'next-sanity'

    export const client = createClient({
      projectId: process.env.NEXT_PUBLIC_SANITY_PROJECT_ID!,
      dataset: process.env.NEXT_PUBLIC_SANITY_DATASET || 'production',
      apiVersion: '2025-04-05',
      useCdn: true,
      stega: {
        enabled: false,
        studioUrl: '/studio',
      },
    })
    ```

    stega.enabled: false par défaut — sera activé en phase 10 pour le visual editing.
  </action>
  <verify>cat sanity/lib/client.ts</verify>
  <done>Client Sanity Next.js créé</done>
</task>

<task type="auto">
  <name>Task 3: Créer sanity/lib/fetch.ts</name>
  <files>sanity/lib/fetch.ts</files>
  <action>
    ```ts
    import { defineLive } from 'next-sanity'
    import { client } from './client'

    const token = process.env.SANITY_API_READ_TOKEN

    export const { sanityFetch, SanityLive } = defineLive({
      client,
      serverToken: token,
      browserToken: token,
      fetchOptions: {
        revalidate: 0,
      },
    })
    ```

    sanityFetch retourne automatiquement les drafts quand le draft mode cookie est actif.
    revalidate: 0 avec les tags Sanity gère l'ISR via le webhook.
  </action>
  <verify>cat sanity/lib/fetch.ts</verify>
  <done>AC-1 satisfait : sanityFetch configuré</done>
</task>

<task type="auto">
  <name>Task 4: Créer sanity/lib/queries.ts</name>
  <files>sanity/lib/queries.ts</files>
  <action>
    Copier le contenu intégral de src/lib/sanityQueries.ts vers sanity/lib/queries.ts.
    Ajouter également la query page builder depuis src/lib/pageBuilderQueries.ts.

    Le fichier doit exporter toutes les queries existantes :
    - homepageQuery
    - instructorsQuery
    - scheduleQuery
    - pricingQuery
    - siteInfoQuery
    - disciplinesQuery
    - venuesQuery
    - registrationInfoQuery
    - newsQuery
    - pageTextsQuery
    - pageQuery (depuis pageBuilderQueries.ts)

    Aucune modification du contenu des queries — copie exacte.
  </action>
  <verify>grep "export const" sanity/lib/queries.ts | wc -l — doit afficher 11</verify>
  <done>Toutes les queries disponibles côté serveur</done>
</task>

<task type="auto">
  <name>Task 5: Créer sanity/lib/live.ts</name>
  <files>sanity/lib/live.ts</files>
  <action>
    ```ts
    export { SanityLive } from './fetch'
    ```

    Re-export simple — SanityLive est défini dans fetch.ts et utilisé dans app/layout.tsx en phase 10.
  </action>
  <verify>cat sanity/lib/live.ts</verify>
  <done>SanityLive exporté</done>
</task>

<task type="auto">
  <name>Task 6: Créer app/api/revalidate/route.ts</name>
  <files>app/api/revalidate/route.ts</files>
  <action>
    ```ts
    import { revalidateTag } from 'next/cache'
    import { type NextRequest, NextResponse } from 'next/server'
    import { parseBody } from 'next-sanity/webhook'

    export async function POST(req: NextRequest) {
      try {
        const { isValidSignature, body } = await parseBody<{ _type: string }>(
          req,
          process.env.SANITY_REVALIDATE_SECRET
        )

        if (!isValidSignature) {
          return NextResponse.json({ message: 'Invalid signature' }, { status: 401 })
        }

        revalidateTag('sanity')
        return NextResponse.json({ revalidated: true, body })
      } catch (err) {
        return NextResponse.json(
          { message: err instanceof Error ? err.message : 'Unknown error' },
          { status: 500 }
        )
      }
    }
    ```

    revalidateTag('sanity') invalide tout le cache Sanity — toutes les pages se regénèrent.
  </action>
  <verify>curl -X POST http://localhost:5247/api/revalidate — doit retourner 401 (pas de signature)</verify>
  <done>AC-2 satisfait : webhook sécurisé</done>
</task>

<task type="auto">
  <name>Task 7: Créer app/api/draft/enable/route.ts</name>
  <files>app/api/draft/enable/route.ts</files>
  <action>
    ```ts
    import { defineEnableDraftMode } from 'next-sanity/draft-mode'
    import { client } from '../../../../sanity/lib/client'

    export const { GET } = defineEnableDraftMode({
      client: client.withConfig({ token: process.env.SANITY_API_READ_TOKEN }),
    })
    ```

    defineEnableDraftMode vérifie le secret et pose le cookie draft mode Next.js.
    Redirige vers / après activation.
  </action>
  <verify>cat app/api/draft/enable/route.ts</verify>
  <done>AC-3 satisfait : draft mode activable</done>
</task>

<task type="auto">
  <name>Task 8: Créer app/api/draft/disable/route.ts</name>
  <files>app/api/draft/disable/route.ts</files>
  <action>
    ```ts
    import { draftMode } from 'next/headers'
    import { NextResponse } from 'next/server'

    export async function GET() {
      const draft = await draftMode()
      draft.disable()
      return NextResponse.redirect(new URL('/', process.env.NEXT_PUBLIC_BASE_URL || 'http://localhost:5247'))
    }
    ```
  </action>
  <verify>cat app/api/draft/disable/route.ts</verify>
  <done>AC-4 satisfait : draft mode désactivable</done>
</task>

<task type="auto">
  <name>Task 9: Mettre à jour .env.local</name>
  <files>.env.local</files>
  <action>
    Ajouter les nouvelles variables dans .env.local :

    ```
    NEXT_PUBLIC_SANITY_PROJECT_ID=z0uyxhwg
    NEXT_PUBLIC_SANITY_DATASET=production
    NEXT_PUBLIC_BASE_URL=http://localhost:5247

    # Obtenir sur sanity.io → projet z0uyxhwg → API → Tokens → Add token (Viewer)
    SANITY_API_READ_TOKEN=

    # Générer avec : openssl rand -base64 32
    SANITY_REVALIDATE_SECRET=
    ```

    Laisser SANITY_API_READ_TOKEN et SANITY_REVALIDATE_SECRET vides pour l'instant — ils seront remplis lors du déploiement Vercel (phase 11) et optionnellement en dev.

    Vérifier que .env.local est dans .gitignore.
  </action>
  <verify>grep "SANITY_API_READ_TOKEN" .env.local && grep ".env.local" .gitignore</verify>
  <done>Variables d'env documentées</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/hooks/useSanity.ts — conservé jusqu'à 09-02
- src/lib/sanityQueries.ts — conservé (source de vérité jusqu'à migration complète)
- app/*/page.tsx — pas de conversion Server Components (c'est 09-02)
- sanity/schemaTypes/ — schemas inchangés

## SCOPE LIMITS
- Ne pas convertir les pages en Server Components (c'est 09-02)
- Ne pas activer stega pour le visual editing (c'est phase 10)
- Ne pas configurer le webhook sur sanity.io (c'est phase 11)

</boundaries>

<verification>
- [ ] `npm run build` sans erreur
- [ ] sanity/lib/fetch.ts, client.ts, queries.ts, live.ts présents
- [ ] 3 routes API créées (revalidate, draft enable, draft disable)
- [ ] .env.local contient les 5 variables (2 remplies, 3 vides documentées)
- [ ] .env.local dans .gitignore
</verification>

<success_criteria>
- Couche data server-side en place
- API routes fonctionnelles (testables via curl)
- Build sans erreur
</success_criteria>

<output>
Après complétion, créer `.paul/phases/09-data-layer/09-01-SUMMARY.md`
</output>
