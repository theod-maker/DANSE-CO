---
phase: 01-responsive
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/AppNavbar.tsx
  - src/pages/AsmeIndex.tsx
  - src/components/planning/ScheduleGrid.tsx
autonomous: true
---

<objective>
## Goal
Corriger les problèmes structurels : menu mobile navbar sans scroll, débordements du hero, et ScheduleGrid mal proportionné sur mobile.

## Purpose
Ces 3 fichiers ont des problèmes de position/débordement plus complexes que les simples ajustements de taille. La navbar mobile peut dépasser l'écran si le menu est long, le hero a un `-translate-y-[20%]` inconditionnelle qui peut couper le CTA sur petit écran, et le ScheduleGrid a des gaps et paddings non adaptés.

## Output
3 fichiers corrigés — navigation mobile fluide, hero lisible sur 375px, planning correct sur mobile.
</objective>

<context>
@.paul/PROJECT.md
@src/components/layout/AppNavbar.tsx
@src/pages/AsmeIndex.tsx
@src/components/planning/ScheduleGrid.tsx
</context>

<acceptance_criteria>

## AC-1: Navbar mobile — menu scrollable et bien dimensionné
```gherkin
Given un navigateur en 375px de large
When on ouvre le menu hamburger
Then le menu apparaît correctement, est scrollable si nécessaire, et les liens sont lisibles
```

## AC-2: Hero AsmeIndex — CTA visible sans débordement sur 375px
```gherkin
Given un navigateur en 375px de large en mode portrait
When on charge la page d'accueil
Then le titre, le champ email, la description et le bouton "Voir le planning" sont tous visibles sans scroll horizontal
```

## AC-3: ScheduleGrid — planning lisible sur mobile
```gherkin
Given un navigateur en 375px de large
When on visite /planning
Then les boutons de jours, les items de cours et les informations sont lisibles et correctement espacés
```

## AC-4: Build TypeScript sans erreur
```gherkin
Given les fichiers modifiés
When on lance npm run build
Then la commande se termine avec "✓ built in" sans erreur
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1 — AppNavbar : menu mobile max-height et paddings</name>
  <files>src/components/layout/AppNavbar.tsx</files>
  <action>
    Menu mobile (le div AnimatePresence à top-[88px]) :
    - Ajouter `max-h-[calc(100vh-100px)] overflow-y-auto` sur le container du menu
    - `rounded-3xl p-6` → `rounded-3xl p-4 md:p-6`
    - Liens menu `text-base` → `text-sm md:text-base`

    Ne pas toucher à la navbar principale ni aux animations d'ouverture/fermeture.
  </action>
  <verify>npm run build passe sans erreur</verify>
  <done>AC-1 satisfait</done>
</task>

<task type="auto">
  <name>Task 2 — AsmeIndex hero : débordements sur petit écran</name>
  <files>src/pages/AsmeIndex.tsx</files>
  <action>
    Hero section — container principal du contenu (`-translate-y-[20%]`) :
    - `-translate-y-[20%]` → `md:-translate-y-[20%]` (pas de translation sur mobile)

    Input email container :
    - `w-full max-w-md` → `w-full max-w-[calc(100vw-3rem)] md:max-w-md`

    Ne pas toucher aux animations, couleurs, orbes ni à la structure globale.
  </action>
  <verify>npm run build passe sans erreur</verify>
  <done>AC-2 satisfait</done>
</task>

<task type="auto">
  <name>Task 3 — ScheduleGrid : boutons, items et gaps mobile</name>
  <files>src/components/planning/ScheduleGrid.tsx</files>
  <action>
    Lire d'abord le fichier pour identifier les classes exactes, puis appliquer :
    - Boutons jours (`px-5 py-2`) → `px-3 py-1.5 md:px-5 md:py-2`
    - Items cours padding (`p-6`) → `p-4 md:p-6`
    - Gap infos dans un item (`gap-4`) → `gap-2 md:gap-4`
    - Texte titre cours si `text-lg` → `text-base md:text-lg`

    Ne pas modifier la logique de filtrage par jour ni les données.
  </action>
  <verify>npm run build passe sans erreur</verify>
  <done>AC-3 et AC-4 satisfaits</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Logique d'ouverture/fermeture du menu (useState, AnimatePresence)
- Animations framer-motion dans AsmeIndex
- Données et logique de filtrage dans ScheduleGrid
- Couleurs et police partout
- Tous les fichiers des plans 01-01 et 01-02

## SCOPE LIMITS
- Ce plan couvre uniquement les 3 fichiers listés
- Pas de refonte de la navbar desktop
- Pas de modification du contenu ou des données

</boundaries>

<verification>
- [ ] `npm run build` passe sans erreur
- [ ] AppNavbar menu mobile : `max-h` + `overflow-y-auto` présents
- [ ] AsmeIndex hero : `-translate-y-[20%]` uniquement sur `md:`
- [ ] ScheduleGrid : paddings et gaps avec échelon mobile
</verification>

<success_criteria>
- 3 fichiers modifiés uniquement sur les aspects structurels listés
- Build TypeScript propre
- Menu mobile non tronqué, hero CTA visible sur 375px, planning lisible
</success_criteria>

<output>
Après complétion : créer `.paul/phases/01-responsive/01-03-SUMMARY.md`
</output>
