---
phase: 01-responsive
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Disciplines.tsx
  - src/pages/Instructors.tsx
  - src/components/locations/VenueCard.tsx
  - src/components/contact/ContactForm.tsx
  - src/components/asme/FeaturedVideoSection.tsx
  - src/components/asme/ServicesSection.tsx
  - src/components/asme/PhilosophySection.tsx
  - src/components/layout/AppFooter.tsx
autonomous: true
---

<objective>
## Goal
Réduire les paddings, gaps et espacements trop grands sur mobile dans les cards, formulaires et sections.

## Purpose
Sur mobile, `p-8` (2rem) dans les cards et `gap-x-8` dans le footer compressent le contenu et dégradent la lisibilité. Ajouter un échelon `sm:` ou réduire la base corrige le layout sans altérer le rendu desktop.

## Output
8 fichiers modifiés — paddings et gaps corrects dès 375px.
</objective>

<context>
@.paul/PROJECT.md
@src/pages/Disciplines.tsx
@src/pages/Instructors.tsx
@src/components/locations/VenueCard.tsx
@src/components/contact/ContactForm.tsx
@src/components/asme/FeaturedVideoSection.tsx
@src/components/asme/ServicesSection.tsx
@src/components/asme/PhilosophySection.tsx
@src/components/layout/AppFooter.tsx
</context>

<acceptance_criteria>

## AC-1: Cards — contenu non compressé sur mobile
```gherkin
Given un navigateur en 375px de large
When on visite /disciplines, /instructors, /locations
Then les cards ont un padding interne suffisant sans débordement ni texte coupé
```

## AC-2: Formulaire contact — champs lisibles sur mobile
```gherkin
Given un navigateur en 375px de large
When on visite /contact
Then les champs du formulaire et le bouton Envoyer sont correctement dimensionnés
```

## AC-3: Sections homepage — gaps proportionnés sur mobile
```gherkin
Given un navigateur en 375px de large
When on scrolle la homepage
Then FeaturedVideoSection, PhilosophySection et ServicesSection ne montrent pas de gaps excessifs
```

## AC-4: Footer — liens non trop espacés sur mobile
```gherkin
Given un navigateur en 375px de large
When on regarde le footer
Then les liens de navigation sont lisibles et ne débordent pas
```

## AC-5: Build TypeScript sans erreur
```gherkin
Given les fichiers modifiés
When on lance npm run build
Then la commande se termine avec "✓ built in" sans erreur
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1 — Paddings cards : Disciplines, Instructors, VenueCard, ServicesSection</name>
  <files>
    src/pages/Disciplines.tsx,
    src/pages/Instructors.tsx,
    src/components/locations/VenueCard.tsx,
    src/components/asme/ServicesSection.tsx
  </files>
  <action>
    Disciplines.tsx — DisciplineCard :
    - `p-8` → `p-5 md:p-8`

    Instructors.tsx — InstructorCard :
    - `p-8` → `p-5 md:p-8`
    - Titre card `text-3xl` → `text-2xl md:text-3xl`

    VenueCard.tsx :
    - `p-8 md:p-10` → `p-5 md:p-8 lg:p-10`
    - Titre `text-3xl md:text-4xl` → `text-2xl md:text-3xl lg:text-4xl`

    ServicesSection.tsx — ServiceCard :
    - `p-6 md:p-8` → `p-4 md:p-6 lg:p-8`

    Ne pas toucher aux animations, couleurs ou structure HTML.
  </action>
  <verify>npm run build passe sans erreur</verify>
  <done>AC-1 satisfait</done>
</task>

<task type="auto">
  <name>Task 2 — Formulaire, Footer, PhilosophySection, FeaturedVideoSection</name>
  <files>
    src/components/contact/ContactForm.tsx,
    src/components/layout/AppFooter.tsx,
    src/components/asme/PhilosophySection.tsx,
    src/components/asme/FeaturedVideoSection.tsx
  </files>
  <action>
    ContactForm.tsx — champs input/textarea :
    - `px-5 py-3.5` → `px-3 py-2.5 md:px-5 md:py-3.5`
    - Bouton submit `px-8 py-3.5` → `px-6 py-3 md:px-8 md:py-3.5`

    AppFooter.tsx :
    - `gap-x-8 gap-y-3` (liens nav) → `gap-x-4 gap-y-3 md:gap-x-8`
    - `gap-8` (flex principal) → `gap-5 md:gap-8`

    PhilosophySection.tsx :
    - `gap-8 md:gap-12` (grid) → `gap-5 md:gap-8 lg:gap-12`

    FeaturedVideoSection.tsx — overlay bottom :
    - `p-6 md:p-10` → `p-4 md:p-6 lg:p-10`
    - `gap-6` → `gap-3 md:gap-6`
    - Box contenu interne `p-6 md:p-8` → `p-4 md:p-6 lg:p-8`

    Ne pas toucher aux animations, couleurs, structure HTML ni logique.
  </action>
  <verify>npm run build passe sans erreur</verify>
  <done>AC-2, AC-3, AC-4, AC-5 satisfaits</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Couleurs, animations, structure HTML
- src/pages/AsmeIndex.tsx (couvert par plan 01-03)
- src/components/layout/AppNavbar.tsx (couvert par plan 01-03)
- src/components/planning/ScheduleGrid.tsx (couvert par plan 01-03)
- Tout fichier non listé dans files_modified

## SCOPE LIMITS
- Ce plan couvre uniquement paddings, gaps et tailles de texte dans les cards
- Pas de modifications sur les grilles ou positions fixed/absolute

</boundaries>

<verification>
- [ ] `npm run build` passe sans erreur
- [ ] Aucune card ne contient de `p-8` ou `p-6` comme valeur de base sans échelon mobile réduit
- [ ] AppFooter : gap-x réduit sur mobile
- [ ] FeaturedVideoSection : overlay sans padding excessif sur mobile
</verification>

<success_criteria>
- 8 fichiers modifiés uniquement sur paddings/gaps/tailles de texte dans cards
- Build TypeScript propre
- Layout correct sur 375px sans compression ni débordement
</success_criteria>

<output>
Après complétion : créer `.paul/phases/01-responsive/01-02-SUMMARY.md`
</output>
