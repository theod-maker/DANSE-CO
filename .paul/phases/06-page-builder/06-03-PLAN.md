---
phase: 06-page-builder
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - src/components/blocks/DisciplinesGridBlock.tsx
  - src/components/blocks/InstructorsGridBlock.tsx
  - src/components/blocks/ScheduleBlock.tsx
  - src/components/blocks/PricingBlock.tsx
  - src/components/blocks/VenuesBlock.tsx
  - src/components/blocks/NewsBlock.tsx
  - src/components/blocks/ContactBlock.tsx
  - src/components/blocks/BlockRenderer.tsx
autonomous: false
---

<objective>
## Goal
Implémenter les 7 blocs de données — chacun récupère ses données depuis Sanity via les hooks existants et rend l'UI correspondante.

## Purpose
Ces blocs transforment les sections dynamiques du site (planning, tarifs, disciplines...) en blocs réutilisables et plaçables n'importe où sur n'importe quelle page.

## Output
- 7 composants blocs dans `src/components/blocks/`
- BlockRenderer mis à jour pour rendre ces blocs
</objective>

<context>
@.paul/PROJECT.md
@src/hooks/useSanity.ts
@src/pages/Disciplines.tsx
@src/pages/Instructors.tsx
@src/pages/Planning.tsx
@src/pages/Pricing.tsx
@src/pages/Locations.tsx
@src/pages/AsmeIndex.tsx
@src/pages/Contact.tsx
@src/components/blocks/BlockRenderer.tsx
@.paul/phases/06-page-builder/06-02-SUMMARY.md
</context>

<acceptance_criteria>

## AC-1: Blocs de données rendus correctement
```gherkin
Given une page avec un bloc DisciplinesGrid
When la page se charge
Then la grille des disciplines s'affiche identique à la page /disciplines actuelle
```

## AC-2: Blocs réutilisables
```gherkin
Given une page quelconque avec un bloc Planning
When la page se charge
Then le planning s'affiche avec les mêmes données que /planning
```

## AC-3: BlockRenderer complet
```gherkin
Given un tableau de blocs contenant tous les types
When BlockRenderer les reçoit
Then chaque type est rendu (aucun cas non géré)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1 — Blocs de données</name>
  <files>
    src/components/blocks/DisciplinesGridBlock.tsx,
    src/components/blocks/InstructorsGridBlock.tsx,
    src/components/blocks/ScheduleBlock.tsx,
    src/components/blocks/PricingBlock.tsx,
    src/components/blocks/VenuesBlock.tsx,
    src/components/blocks/NewsBlock.tsx,
    src/components/blocks/ContactBlock.tsx
  </files>
  <action>
    Chaque composant reçoit ses props (_type, _key) et récupère ses données via les hooks existants.

    **DisciplinesGridBlock.tsx** — extraire la grille DisciplineCard de Disciplines.tsx dans ce composant autonome. Appelle `useDisciplines()` en interne. Rendu identique (grid md:grid-cols-2 gap-6, même DisciplineCard).

    **InstructorsGridBlock.tsx** — extraire InstructorCard + grille de Instructors.tsx. Appelle `useInstructors()`.

    **ScheduleBlock.tsx** — importer et rendre `<ScheduleGrid />` + `<RegistrationInfo />` existants. Appelle `useSiteInfo()` pour la saison si nécessaire.

    **PricingBlock.tsx** — extraire le tableau des tarifs de Pricing.tsx dans ce composant. Appelle `usePricing()`.

    **VenuesBlock.tsx** — extraire la liste des VenueCard de Locations.tsx. Appelle `useVenues()`.

    **NewsBlock.tsx** — réutiliser la logique de NewsSection.tsx. Appelle `useNews()`. Retourne null si pas d'actus.

    **ContactBlock.tsx** — extraire le grid (formulaire + infos) de Contact.tsx. Appelle `useSiteInfo()`.

    Règle : chaque composant est autonome (fetch ses propres données), wrappé dans une section avec les paddings standard du site (py-20 md:py-28 px-6 max-w-6xl mx-auto).
  </action>
  <verify>npx tsc --noEmit → 0 erreur</verify>
  <done>AC-1 et AC-2 partiellement satisfaits</done>
</task>

<task type="auto">
  <name>Task 2 — BlockRenderer mis à jour</name>
  <files>src/components/blocks/BlockRenderer.tsx</files>
  <action>
    Remplacer les placeholders `<div data-block-type>` par les vrais imports et rendus des 7 blocs complexes.

    ```tsx
    case 'disciplinesGridBlock': return <DisciplinesGridBlock key={block._key} />
    case 'instructorsGridBlock': return <InstructorsGridBlock key={block._key} />
    case 'scheduleBlock': return <ScheduleBlock key={block._key} />
    case 'pricingBlock': return <PricingBlock key={block._key} />
    case 'venuesBlock': return <VenuesBlock key={block._key} />
    case 'newsBlock': return <NewsBlock key={block._key} />
    case 'contactBlock': return <ContactBlock key={block._key} />
    ```
  </action>
  <verify>npx tsc --noEmit → 0 erreur</verify>
  <done>AC-3 satisfait</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>7 blocs de données + BlockRenderer complet</what-built>
  <how-to-verify>
    1. `npx tsc --noEmit` → 0 erreur
    2. `npm run dev` → toutes les pages existantes fonctionnent normalement
    3. Les 14 composants existent dans src/components/blocks/
  </how-to-verify>
  <resume-signal>Tape "approved" ou décris les problèmes</resume-signal>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- Pages React existantes — migration en 06-04
- Composants ScheduleGrid, RegistrationInfo, VenueCard — réutilisés tels quels
- Hooks existants dans useSanity.ts

## SCOPE LIMITS
- Pas de migration des pages dans ce plan
- Les blocs complexes réutilisent les composants existants sans les refactorer
</boundaries>

<verification>
- [ ] 14 composants dans src/components/blocks/
- [ ] npx tsc --noEmit → 0 erreur
- [ ] Site toujours fonctionnel
</verification>

<success_criteria>
- BlockRenderer complet — tous les types gérés
- 0 erreur TypeScript
- Site existant non impacté
</success_criteria>

<output>
Créer `.paul/phases/06-page-builder/06-03-SUMMARY.md` après completion
</output>
