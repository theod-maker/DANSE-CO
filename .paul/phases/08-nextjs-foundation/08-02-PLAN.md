---
phase: 08-nextjs-foundation
plan: 02
type: execute
wave: 1
depends_on: ["08-01"]
files_modified:
  - app/disciplines/page.tsx
  - app/instructors/page.tsx
  - app/pricing/page.tsx
  - app/planning/page.tsx
  - app/locations/page.tsx
  - app/contact/page.tsx
  - app/histoire/page.tsx
  - app/actualites/page.tsx
  - app/page.tsx
  - app/studio/[[...tool]]/page.tsx
  - src/components/layout/AppNavbar.tsx
  - src/components/layout/AppFooter.tsx
  - src/lib/sanityClient.ts
autonomous: true
---

<objective>
## Goal
Migrer les 9 pages src/pages → app/*/page.tsx, migrer le Studio Sanity, adapter les composants layout pour Next.js (Link, usePathname), et supprimer Vite.

## Purpose
Rendre le site entièrement fonctionnel sous Next.js. Les hooks useSanity sont conservés temporairement avec 'use client' — ils seront remplacés par des Server Components en phase 09.

## Output
- 9 pages dans app/ (toutes avec 'use client' temporairement)
- app/studio/[[...tool]]/page.tsx fonctionnel
- AppNavbar + AppFooter adaptés pour next/link et next/navigation
- sanityClient.ts adapté pour variables NEXT_PUBLIC_
- Vite supprimé (index.html, vite.config.ts, src/main.tsx, src/App.tsx)
- Build Next.js sans erreur, toutes les pages accessibles
</objective>

<context>
@.paul/PROJECT.md
@src/components/layout/AppNavbar.tsx
@src/pages/Disciplines.tsx

## Pages à migrer
- AsmeIndex.tsx → app/page.tsx
- Disciplines.tsx → app/disciplines/page.tsx
- Instructors.tsx → app/instructors/page.tsx
- Pricing.tsx → app/pricing/page.tsx
- Planning.tsx → app/planning/page.tsx
- Locations.tsx → app/locations/page.tsx
- Contact.tsx → app/contact/page.tsx
- Histoire.tsx → app/histoire/page.tsx
- Actualites.tsx → app/actualites/page.tsx

## Changements systématiques par page
1. Ajouter 'use client' en première ligne (hooks useState/useEffect incompatibles Server Components)
2. Supprimer les imports CSS modules inline si présents (Contact.module.css, Disciplines.module.css, Pricing.module.css — les conserver dans src/ pour l'instant)
3. Remplacer les imports de composants (pas de changement de chemin nécessaire si on garde src/components/)
</context>

<acceptance_criteria>

## AC-1: Toutes les pages accessibles
```gherkin
Given le build Next.js complété
When on visite chaque route (/, /disciplines, /instructors, /pricing, /planning, /locations, /contact, /histoire, /actualites)
Then chaque page s'affiche sans erreur 404 ni erreur JS console
```

## AC-2: Studio Sanity accessible
```gherkin
Given app/studio/[[...tool]]/page.tsx créé
When on visite http://localhost:5247/studio
Then le Studio Sanity se charge correctement
```

## AC-3: Navigation fonctionnelle
```gherkin
Given AppNavbar adapté avec next/link
When on clique sur un lien de navigation
Then la navigation se fait sans rechargement complet de page (SPA behavior)
```

## AC-4: Vite supprimé
```gherkin
Given la migration complète
When on lance `npm run dev`
Then Next.js démarre (pas Vite) et index.html n'existe plus à la racine
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Adapter AppNavbar pour next/link + usePathname</name>
  <files>src/components/layout/AppNavbar.tsx</files>
  <action>
    Remplacer les imports react-router-dom par les équivalents Next.js :

    ```tsx
    'use client'
    import { useState } from 'react';
    import Link from 'next/link';
    import { usePathname } from 'next/navigation';
    ```

    Remplacer `useLocation()` par `usePathname()` :
    - Avant : `const location = useLocation();`
    - Après : `const pathname = usePathname();`

    Remplacer toutes les occurrences de `location.pathname` par `pathname`.

    Remplacer `<Link to="...">` par `<Link href="...">` partout dans le fichier.
    Next.js Link ne wrap pas un `<a>` — garder le contenu directement dans `<Link>`.
  </action>
  <verify>grep -n "react-router-dom\|useLocation\|location.pathname" src/components/layout/AppNavbar.tsx — doit retourner vide</verify>
  <done>AC-3 satisfait : navigation next/link fonctionnelle</done>
</task>

<task type="auto">
  <name>Task 2: Adapter AppFooter pour next/link</name>
  <files>src/components/layout/AppFooter.tsx</files>
  <action>
    Ajouter 'use client' si le composant utilise des hooks, sinon juste adapter les imports.
    Remplacer `import { Link } from 'react-router-dom'` par `import Link from 'next/link'`.
    Remplacer `<Link to="...">` par `<Link href="...">` partout.
  </action>
  <verify>grep "react-router-dom" src/components/layout/AppFooter.tsx — doit retourner vide</verify>
  <done>Footer compatible Next.js</done>
</task>

<task type="auto">
  <name>Task 3: Adapter sanityClient.ts pour variables NEXT_PUBLIC_</name>
  <files>src/lib/sanityClient.ts</files>
  <action>
    Remplacer les variables d'env Vite par les variables Next.js :

    ```ts
    import { createClient } from '@sanity/client'

    const projectId = process.env.NEXT_PUBLIC_SANITY_PROJECT_ID
    const dataset = process.env.NEXT_PUBLIC_SANITY_DATASET || 'production'
    const apiVersion = '2025-04-05'

    export const sanityClient = createClient({
      projectId: projectId || 'z0uyxhwg',
      dataset,
      apiVersion,
      useCdn: true,
    })

    export const isSanityConfigured = true
    ```

    Utiliser le project ID hardcodé comme fallback (z0uyxhwg) pour simplifier — les variables d'env seront configurées en phase 11.
    Supprimer le check `isSanityConfigured` (toujours true désormais).
  </action>
  <verify>grep "import.meta.env" src/lib/sanityClient.ts — doit retourner vide</verify>
  <done>Client Sanity compatible Next.js</done>
</task>

<task type="auto">
  <name>Task 4: Migrer les 9 pages vers app/</name>
  <files>
    app/page.tsx,
    app/disciplines/page.tsx,
    app/instructors/page.tsx,
    app/pricing/page.tsx,
    app/planning/page.tsx,
    app/locations/page.tsx,
    app/contact/page.tsx,
    app/histoire/page.tsx,
    app/actualites/page.tsx
  </files>
  <action>
    Pour chaque page, créer le fichier dans app/ avec ces transformations :

    1. Ajouter 'use client' en première ligne absolue
    2. Corriger les chemins d'import (src/pages/ → app/, donc les imports relatifs changent) :
       - '../components/...' → '../../src/components/...' OU déplacer les imports
       - Plus simple : utiliser des chemins depuis la racine avec @/ alias
       - Exemple : '@/src/components/layout/AppNavbar'
    3. Remplacer `import { Link } from 'react-router-dom'` par `import Link from 'next/link'`
    4. Remplacer `<Link to="...">` par `<Link href="...">`
    5. Supprimer les imports de CSS modules (Contact.module.css etc.) — les classes sont référencées par nom de variable, vérifier si utilisées
    6. Garder tous les hooks useSanity, useHomepage, etc. inchangés

    app/page.tsx : contenu de src/pages/AsmeIndex.tsx
    app/disciplines/page.tsx : contenu de src/pages/Disciplines.tsx
    app/instructors/page.tsx : contenu de src/pages/Instructors.tsx
    app/pricing/page.tsx : contenu de src/pages/Pricing.tsx
    app/planning/page.tsx : contenu de src/pages/Planning.tsx
    app/locations/page.tsx : contenu de src/pages/Locations.tsx
    app/contact/page.tsx : contenu de src/pages/Contact.tsx
    app/histoire/page.tsx : contenu de src/pages/Histoire.tsx
    app/actualites/page.tsx : contenu de src/pages/Actualites.tsx

    Mettre à jour tsconfig.json pour ajouter l'alias :
    ```json
    "paths": {
      "@/*": ["./*"]
    }
    ```
  </action>
  <verify>
    `npm run build` — vérifier que les 9 routes apparaissent dans l'output Next.js
    `npm run dev` puis tester chaque route manuellement
  </verify>
  <done>AC-1 satisfait : 9 pages accessibles</done>
</task>

<task type="auto">
  <name>Task 5: Créer la route Studio Sanity</name>
  <files>app/studio/[[...tool]]/page.tsx</files>
  <action>
    Créer app/studio/[[...tool]]/page.tsx :

    ```tsx
    'use client'

    import { NextStudio } from 'next-sanity/studio'
    import config from '../../../sanity.config'

    export default function StudioPage() {
      return <NextStudio config={config} />
    }
    ```

    Installer next-sanity si absent : `npm install next-sanity`
    next-sanity fournit NextStudio qui gère le rendu du Studio dans Next.js App Router.
  </action>
  <verify>http://localhost:5247/studio charge le Studio Sanity</verify>
  <done>AC-2 satisfait : Studio accessible</done>
</task>

<task type="auto">
  <name>Task 6: Supprimer Vite et valider le build final</name>
  <files>
    index.html,
    vite.config.ts,
    src/main.tsx,
    src/App.tsx
  </files>
  <action>
    1. Supprimer les fichiers Vite :
       - `rm index.html`
       - `rm vite.config.ts`
       - `rm src/main.tsx`
       - `rm src/App.tsx`

    2. Créer .env.local à la racine (renommer .env si existant) :
       ```
       NEXT_PUBLIC_SANITY_PROJECT_ID=z0uyxhwg
       NEXT_PUBLIC_SANITY_DATASET=production
       ```

    3. Lancer `npm run build` — corriger toute erreur TypeScript résiduelle
    4. Lancer `npm run dev` — vérifier que toutes les routes répondent
  </action>
  <verify>
    - `ls index.html` → "No such file"
    - `ls vite.config.ts` → "No such file"
    - `npm run build` sans erreur
  </verify>
  <done>AC-4 satisfait : Vite supprimé, Next.js seul</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- sanity/schemaTypes/ — schemas Sanity inchangés
- src/components/ — composants UI inchangés (sauf AppNavbar + AppFooter)
- src/lib/fallbackContent.ts — fallbacks inchangés
- src/lib/sanityQueries.ts — queries GROQ inchangées

## SCOPE LIMITS
- Ne pas convertir en Server Components (c'est 09-02)
- Ne pas installer @sanity/presentation ni @sanity/visual-editing (c'est phase 10)
- Ne pas configurer les API routes revalidate/draft (c'est 09-01)
- Conserver src/pages/ intact (ne pas supprimer, juste ne plus utiliser)

</boundaries>

<verification>
- [ ] `npm run build` sans erreur TypeScript
- [ ] Toutes les 9 routes accessibles en dev
- [ ] /studio charge le Studio Sanity
- [ ] Navigation entre pages sans rechargement complet
- [ ] Aucune référence import.meta.env dans le code actif
- [ ] Vite supprimé (index.html absent)
</verification>

<success_criteria>
- Site entièrement fonctionnel sous Next.js
- Toutes les pages affichent le même contenu qu'avant
- Studio Sanity accessible sur /studio
- Vite entièrement retiré
</success_criteria>

<output>
Après complétion, créer `.paul/phases/08-nextjs-foundation/08-02-SUMMARY.md`
</output>
