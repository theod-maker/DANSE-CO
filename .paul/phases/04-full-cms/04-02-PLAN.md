---
phase: 04-full-cms
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/lib/fallbackContent.ts
  - src/lib/sanityQueries.ts
  - src/hooks/useSanity.ts
autonomous: true
---

<objective>
## Goal
Créer la couche data complète pour les 3 nouveaux schemas : types TypeScript, fallbacks locaux, queries GROQ et hooks React.

## Purpose
Sans cette couche, les composants React ne peuvent pas consommer les nouvelles données Sanity. Ce plan est le pont entre les schemas (04-01) et le branchement des composants (04-03).

## Output
3 fichiers mis à jour. 3 nouvelles interfaces TS exportées, 3 fallbacks, 3 queries GROQ, 3 hooks. Le schema `homepage` reçoit aussi les types pour les nouvelles images.
</objective>

<context>
@.paul/PROJECT.md
@src/lib/fallbackContent.ts
@src/lib/sanityQueries.ts
@src/hooks/useSanity.ts
@sanity/schemaTypes/siteInfo.ts
@sanity/schemaTypes/discipline.ts
@sanity/schemaTypes/venue.ts
</context>

<acceptance_criteria>

## AC-1: Types TypeScript complets
```gherkin
Given les interfaces ajoutées dans fallbackContent.ts
When on importe SiteInfoContent, DisciplineContent, VenueContent dans un composant
Then TypeScript les résout sans erreur et toutes les propriétés sont typées
```

## AC-2: Fallbacks cohérents avec le contenu actuel
```gherkin
Given les fallbacks définis
When Sanity n'est pas configuré (isSanityConfigured = false)
Then les hooks retournent les données hardcodées actuelles du site (ex: disciplines Rock & Roll, venues Le Canopus, coordonnées réelles)
```

## AC-3: Hooks fonctionnels
```gherkin
Given les 3 nouveaux hooks exportés depuis useSanity.ts
When un composant appelle useSiteInfo(), useDisciplines(), useVenues()
Then le hook retourne le fallback par défaut, puis les données Sanity si disponibles
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter types, interfaces et fallbacks dans fallbackContent.ts</name>
  <files>src/lib/fallbackContent.ts</files>
  <action>
    Mettre à jour l'interface HomepageContent pour ajouter:
    - heroTagline?: string
    - philosophyImageUrl?: string
    - featuredImageUrl?: string
    - featuredSectionLabel?: string
    - servicesCard1ImageUrl?: string
    - servicesCard2ImageUrl?: string

    Ajouter l'interface SiteInfoContent:
    - phone: string
    - email: string
    - mailingAddress: string
    - instagramUrl: string
    - facebookUrl: string
    - twitterUrl: string
    - season: string
    - footerTagline: string

    Ajouter l'interface DisciplineContent:
    - _id: string
    - title: string
    - iconName: 'Zap' | 'Star' | 'Heart' | 'Music'
    - description: string
    - benefits: string[]

    Ajouter l'interface VenueContent:
    - _id: string
    - name: string
    - address: string
    - description: string
    - amenities: string[]
    - mapEmbedUrl: string
    - googleMapsUrl: string

    Ajouter l'interface RegistrationInfoContent:
    - permanence1Days: string
    - permanence1Hours: string
    - permanence1Venue: string
    - permanence2Days: string
    - permanence2Hours: string
    - permanence2Venue: string
    - requiredDocuments: string[]
    - photoNote: string

    Ajouter l'interface PageTextsContent:
    - planningSubtitle: string
    - disciplinesSubtitle: string
    - locationsSubtitle: string
    - contactSubtitle: string
    - instructorsSubtitle: string
    - pricingSubtitle: string

    Ajouter le fallback fallbackSiteInfo (SiteInfoContent) avec les vraies coordonnées actuelles:
    - phone: '06.17.09.93.49', email: 'DANSANDCO@OUTLOOK.FR'
    - mailingAddress: '17 Rue du Chevecier, 44730 Saint-Michel-Chef-Chef'
    - instagramUrl: '', facebookUrl: '', twitterUrl: ''
    - season: '2025–2026'
    - footerTagline: 'Saint-Michel-Chef-Chef · Saison 2025–2026'

    Ajouter fallbackDisciplines (DisciplineContent[]) avec les 4 disciplines hardcodées actuelles:
    - Rock & Roll (iconName: 'Zap'), Danses de Salon ('Star'), Salsa Cubaine ('Heart'), West Coast Swing ('Music')
    - Reprendre exactement les descriptions et benefits déjà dans Disciplines.tsx

    Ajouter fallbackVenues (VenueContent[]) avec les 2 salles hardcodées actuelles:
    - Le Canopus et Salle Caraïbes avec toutes leurs données existantes

    Ajouter fallbackRegistrationInfo (RegistrationInfoContent):
    - permanence1Days: 'Lundis & Mercredis', permanence1Hours: '19h30 – 21h30', permanence1Venue: 'Le Canopus'
    - permanence2Days: 'Samedis', permanence2Hours: '10h00 – 12h00', permanence2Venue: 'Complexe de la Viauderie'
    - requiredDocuments: ['La fiche d\'inscription remplie', 'Un certificat médical (obligatoire)', 'Le règlement de la cotisation']
    - photoNote: 'Une photo d\'identité est nécessaire pour votre carte d\'adhérent (nouveaux membres uniquement).'

    Ajouter fallbackPageTexts (PageTextsContent) avec les textes hardcodés actuels:
    - planningSubtitle: 'Retrouvez tous nos créneaux hebdomadaires. Les cours ont lieu au Canopus et à la Salle Caraïbes.'
    - disciplinesSubtitle: 'Explorez une grande variété de styles de danse, enseignés avec passion et expertise pour tous les niveaux.'
    - locationsSubtitle: 'Deux espaces magnifiques à Saint-Michel-Chef-Chef, adaptés à tous les styles de danse.'
    - contactSubtitle: 'Une question sur nos cours, les inscriptions ou un événement ? Nous sommes là pour vous répondre.'
    - instructorsSubtitle: 'Une équipe de passionnés pour vous accompagner dans votre apprentissage, quel que soit votre niveau.'
    - pricingSubtitle: 'Une tarification simple et transparente. Les tarifs sont annuels et incluent l\'adhésion à l\'association.'
  </action>
  <verify>`npx tsc --noEmit` sans erreur, toutes les nouvelles interfaces et fallbacks exportés.</verify>
  <done>AC-1, AC-2 satisfaits: types complets, fallbacks avec données réelles.</done>
</task>

<task type="auto">
  <name>Task 2: Ajouter queries GROQ et hooks</name>
  <files>src/lib/sanityQueries.ts, src/hooks/useSanity.ts</files>
  <action>
    Dans `sanityQueries.ts`:
    - Mettre à jour homepageQuery pour ajouter:
      heroTagline,
      "philosophyImageUrl": philosophyImage.asset->url,
      "featuredImageUrl": featuredImage.asset->url,
      featuredSectionLabel,
      "servicesCard1ImageUrl": servicesCard1Image.asset->url,
      "servicesCard2ImageUrl": servicesCard2Image.asset->url

    - Ajouter siteInfoQuery:
      `*[_type == "siteInfo" && _id == "siteInfo"][0]{ phone, email, mailingAddress, instagramUrl, facebookUrl, twitterUrl, season, footerTagline }`

    - Ajouter disciplinesQuery:
      `*[_type == "discipline"] | order(order asc) { _id, title, iconName, description, benefits }`

    - Ajouter venuesQuery:
      `*[_type == "venue"] | order(order asc) { _id, name, address, description, amenities, mapEmbedUrl, googleMapsUrl }`

    - Ajouter registrationInfoQuery:
      `*[_type == "registrationInfo" && _id == "registrationInfo"][0]{ permanence1Days, permanence1Hours, permanence1Venue, permanence2Days, permanence2Hours, permanence2Venue, requiredDocuments, photoNote }`

    - Ajouter pageTextsQuery:
      `*[_type == "pageTexts" && _id == "pageTexts"][0]{ planningSubtitle, disciplinesSubtitle, locationsSubtitle, contactSubtitle, instructorsSubtitle, pricingSubtitle }`

    Dans `useSanity.ts`:
    - Importer les 5 nouvelles queries et les 5 nouvelles interfaces/fallbacks
    - Ajouter useSiteInfo(): SiteInfoContent
    - Ajouter useDisciplines(): DisciplineContent[]
    - Ajouter useVenues(): VenueContent[]
    - Ajouter useRegistrationInfo(): RegistrationInfoContent
    - Ajouter usePageTexts(): PageTextsContent
    - Même pattern que les hooks existants (useSanityData générique)
  </action>
  <verify>`npx tsc --noEmit` sans erreur. Les 3 hooks exportés depuis useSanity.ts.</verify>
  <done>AC-3 satisfait: hooks fonctionnels avec fallback et requête Sanity.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/components/ (aucun composant dans ce plan)
- src/pages/ (aucune page dans ce plan)
- sanity/ (schemas déjà créés en 04-01)
- Les exports existants de fallbackContent.ts et useSanity.ts (ne pas casser l'existant)

## SCOPE LIMITS
- Ce plan ne touche qu'à la couche data (types, queries, hooks, fallbacks)
- Ne pas modifier les queries existantes (homepageQuery peut être étendu, pas réécrit)
- Ne pas modifier les hooks existants (useHomepage, useInstructors, useSchedule, usePricing)

</boundaries>

<verification>
- [ ] `npx tsc --noEmit` passe sans erreur
- [ ] `fallbackContent.ts` exporte SiteInfoContent, DisciplineContent, VenueContent, RegistrationInfoContent, PageTextsContent + tous leurs fallbacks
- [ ] `fallbackHomepage` mis à jour avec les nouveaux champs optionnels
- [ ] `sanityQueries.ts` exporte siteInfoQuery, disciplinesQuery, venuesQuery, registrationInfoQuery, pageTextsQuery
- [ ] `homepageQuery` inclut les 6 nouveaux champs
- [ ] `useSanity.ts` exporte useSiteInfo, useDisciplines, useVenues, useRegistrationInfo, usePageTexts
- [ ] Tous les critères d'acceptance vérifiés
</verification>

<success_criteria>
- Tous les tasks complétés
- TypeScript compile sans erreur
- Les hooks existants non cassés
</success_criteria>

<output>
Après complétion, créer `.paul/phases/04-full-cms/04-02-SUMMARY.md`
</output>
