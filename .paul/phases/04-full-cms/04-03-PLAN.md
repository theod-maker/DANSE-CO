---
phase: 04-full-cms
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/pages/Disciplines.tsx
  - src/pages/Locations.tsx
  - src/pages/Contact.tsx
  - src/pages/Planning.tsx
  - src/pages/Instructors.tsx
  - src/pages/Pricing.tsx
  - src/components/layout/AppFooter.tsx
  - src/components/planning/RegistrationInfo.tsx
  - src/components/asme/PhilosophySection.tsx
  - src/components/asme/FeaturedVideoSection.tsx
  - src/components/asme/ServicesSection.tsx
  - src/pages/AsmeIndex.tsx
autonomous: false
---

<objective>
## Goal
Brancher les 7 composants/pages sur les hooks Sanity — supprimer toutes les données hardcodées restantes.

## Purpose
C'est la dernière étape : après ce plan, 100% du contenu visible du site est éditable depuis le studio Sanity sans toucher au code.

## Output
7 fichiers modifiés. Zéro texte, coordonnée, image ou donnée métier hardcodée dans les composants React (sauf les animations et les classes CSS).
</objective>

<context>
@.paul/PROJECT.md
@src/pages/Disciplines.tsx
@src/pages/Locations.tsx
@src/pages/Contact.tsx
@src/components/layout/AppFooter.tsx
@src/components/asme/PhilosophySection.tsx
@src/components/asme/FeaturedVideoSection.tsx
@src/components/asme/ServicesSection.tsx
@src/hooks/useSanity.ts
@src/lib/fallbackContent.ts
</context>

<acceptance_criteria>

## AC-1: Disciplines dynamiques
```gherkin
Given useDisciplines() retournant des données (fallback ou Sanity)
When on visite /disciplines
Then les cartes sont générées dynamiquement depuis les données, les icônes Lucide correspondent à iconName
```

## AC-2: Salles dynamiques
```gherkin
Given useVenues() retournant des données
When on visite /locations
Then les VenueCards sont générées dynamiquement, maps et liens utilisent les données du hook
```

## AC-3: Coordonnées et réseaux dynamiques
```gherkin
Given useSiteInfo() retournant des données
When on visite /contact et que le footer est visible
Then tél, email, adresse postale et liens réseaux sociaux proviennent du hook (non hardcodés)
When instagramUrl/facebookUrl/twitterUrl sont vides
Then les icônes correspondantes sont masquées (pas de lien brisé)
```

## AC-5: Liens sociaux AsmeIndex hero branchés
```gherkin
Given siteInfo.instagramUrl et siteInfo.twitterUrl configurés
When on visite la page d'accueil
Then les boutons sociaux du hero deviennent des liens cliquables vers les URLs Sanity
When les URLs sont vides
Then les boutons sont masqués (pas de bouton cassé)
```

## AC-4: Images homepage depuis Sanity
```gherkin
Given content.philosophyImageUrl, content.featuredImageUrl, content.servicesCard1ImageUrl, content.servicesCard2ImageUrl disponibles
When ces valeurs sont définies dans Sanity
Then les images correspondantes dans PhilosophySection, FeaturedVideoSection et ServicesSection utilisent ces URLs
When les valeurs sont null/undefined (Sanity vide)
Then le composant affiche l'image Unsplash placeholder actuelle
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Brancher Disciplines, Locations, Contact sur les hooks</name>
  <files>src/pages/Disciplines.tsx, src/pages/Locations.tsx, src/pages/Contact.tsx, src/components/layout/AppFooter.tsx</files>
  <action>
    `Disciplines.tsx`:
    - Importer useDisciplines et usePageTexts depuis hooks/useSanity
    - Supprimer le tableau disciplines hardcodé
    - Appeler const disciplines = useDisciplines() et const pageTexts = usePageTexts()
    - Mapper iconName vers le composant Lucide React correspondant:
      créer un objet ICON_MAP = { Zap: Zap, Star: Star, Heart: Heart, Music: Music } et utiliser ICON_MAP[d.iconName]
    - Passer size={32} à l'icône comme actuellement
    - Remplacer le sous-titre hardcodé par pageTexts.disciplinesSubtitle

    `Locations.tsx`:
    - Importer useVenues et usePageTexts depuis hooks/useSanity
    - Supprimer le tableau venues hardcodé
    - Appeler const venues = useVenues() et const pageTexts = usePageTexts()
    - Passer venues au map existant (les props de VenueCard ne changent pas)
    - Remplacer le sous-titre hardcodé par pageTexts.locationsSubtitle

    `Contact.tsx`:
    - Importer useSiteInfo et usePageTexts depuis hooks/useSanity
    - Appeler const siteInfo = useSiteInfo() et const pageTexts = usePageTexts()
    - Remplacer '06.17.09.93.49' par siteInfo.phone, 'DANSANDCO@OUTLOOK.FR' par siteInfo.email
    - Remplacer '17 Rue du Chevecier...' par siteInfo.mailingAddress
    - Les liens réseaux sociaux: siteInfo.instagramUrl et siteInfo.facebookUrl (masquer si vide)
    - Remplacer le sous-titre hardcodé par pageTexts.contactSubtitle

    `AppFooter.tsx`:
    - Importer useSiteInfo
    - Remplacer 'Saint-Michel-Chef-Chef · Saison 2025–2026' par siteInfo.footerTagline
    - Les liens href="#" des icônes sociales: siteInfo.instagramUrl et siteInfo.facebookUrl
      (masquer l'icône si URL vide)

    `AsmeIndex.tsx`:
    - Importer useSiteInfo (en plus de useHomepage déjà présent)
    - Appeler const siteInfo = useSiteInfo()
    - Les 3 boutons sociaux en bas du hero (Instagram, Twitter, Globe) deviennent des <a> conditionnels:
      Instagram → siteInfo.instagramUrl, Twitter → siteInfo.twitterUrl, Globe → masqué si pas d'URL
      Pattern: {siteInfo.instagramUrl && <a href={siteInfo.instagramUrl} target="_blank" rel="noopener noreferrer">...</a>}
    - La tagline "École de Danse · Saint-Michel-Chef-Chef" devient: {content.heroTagline || 'École de Danse · Saint-Michel-Chef-Chef'}

    `Planning.tsx`:
    - Importer useSiteInfo et usePageTexts
    - Remplacer "SAISON 2025 – 2026" par `SAISON {siteInfo.season}`
    - Remplacer le sous-titre hardcodé par pageTexts.planningSubtitle

    `Instructors.tsx`:
    - Importer usePageTexts
    - Remplacer le sous-titre hardcodé par pageTexts.instructorsSubtitle

    `Pricing.tsx`:
    - Importer usePageTexts
    - Remplacer le sous-titre hardcodé par pageTexts.pricingSubtitle
    (Note: "SAISON {pricingData.season}" est déjà dynamique dans Pricing.tsx — ne pas toucher)
  </action>
  <verify>
    npm run build sans erreur TypeScript.
    Visite /disciplines → cartes générées. /locations → salles générées. /contact → coordonnées depuis fallback.
  </verify>
  <done>AC-1, AC-2, AC-3 satisfaits.</done>
</task>

<task type="auto">
  <name>Task 2: Brancher RegistrationInfo + images homepage sub-composants</name>
  <files>src/components/planning/RegistrationInfo.tsx, src/components/asme/PhilosophySection.tsx, src/components/asme/FeaturedVideoSection.tsx, src/components/asme/ServicesSection.tsx</files>
  <action>
    `RegistrationInfo.tsx`:
    - Importer useRegistrationInfo et useSiteInfo depuis hooks/useSanity
    - Appeler const regInfo = useRegistrationInfo() et const siteInfo = useSiteInfo()
    - Supprimer le tableau cards hardcodé
    - Reconstruire les 3 cartes dynamiquement depuis regInfo:
      Carte 1 "Permanences": afficher regInfo.permanence1Days + regInfo.permanence1Hours + regInfo.permanence1Venue,
        et regInfo.permanence2Days + regInfo.permanence2Hours + regInfo.permanence2Venue
      Carte 2 "Dossier Requis": mapper regInfo.requiredDocuments en liste <li>
      Carte 3 "Photo d'identité": afficher regInfo.photoNote
    - Le numéro en bas: remplacer '06.17.09.93.49' et le tel: href par siteInfo.phone
    - Garder les mêmes icônes (AlertCircle, FileText, Camera) et la même structure JSX

    Ces composants reçoivent déjà `content: HomepageContent` en props depuis AsmeIndex.

    `PhilosophySection.tsx`:
    - L'image Unsplash hardcodée devient: src={content.philosophyImageUrl || 'https://images.unsplash.com/photo-1547153760-18fc86324498?w=900&q=85'}

    `FeaturedVideoSection.tsx`:
    - L'image Unsplash hardcodée devient: src={content.featuredImageUrl || 'https://images.unsplash.com/photo-1508700115892-45ecd05ae2ad?w=1400&q=85'}
    - Le label "NOTRE APPROCHE" devient: {content.featuredSectionLabel || 'NOTRE APPROCHE'}

    `ServicesSection.tsx`:
    - Dans le tableau services hardcodé, remplacer les imageUrl:
      Card 1: imageUrl: content.servicesCard1ImageUrl || 'https://images.unsplash.com/photo-1504609773096-104ff2c73ba4?w=900&q=85'
      Card 2: imageUrl: content.servicesCard2ImageUrl || 'https://images.unsplash.com/photo-1535525153316-d772a249b53a?w=900&q=85'

    Ne pas modifier les autres props (imageAlt, tag, title, description, href) ni les animations.
  </action>
  <verify>
    npm run build sans erreur.
    Les fallback Unsplash s'affichent toujours quand les champs Sanity sont vides.
  </verify>
  <done>AC-4 satisfait: images conditionnelles Sanity || placeholder.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Tous les composants branchés sur les hooks Sanity. Le site doit fonctionner identiquement à avant (fallbacks actifs), et les données seront remplacées dès qu'on saisit du contenu dans le studio.
  </what-built>
  <how-to-verify>
    1. Lancer: `npm run dev`
    2. Visiter: http://localhost:5173/disciplines → les 4 disciplines s'affichent normalement
    3. Visiter: http://localhost:5173/locations → les 2 salles s'affichent avec maps
    4. Visiter: http://localhost:5173/contact → tél, email, adresse corrects
    5. Vérifier le footer → "Saison 2025–2026" visible
    6. Lancer: `npm run build` → 0 erreur TypeScript
    7. Optionnel: lancer `npx sanity dev`, modifier une discipline dans le studio → site se met à jour
  </how-to-verify>
  <resume-signal>Taper "approved" si tout est OK, ou décrire les problèmes à corriger</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/components/asme/AboutSection.tsx (déjà branché sur content)
- src/pages/AsmeIndex.tsx (ne pas modifier le passage de content aux sous-composants)
- src/components/locations/VenueCard.tsx (interface props ne change pas)
- sanity/ et src/lib/ (gérés dans 04-01 et 04-02)
- Les animations Framer Motion, classes Tailwind, styles CSS

## SCOPE LIMITS
- Ne remplacer que les données métier hardcodées — pas de refactor des composants
- Ne pas changer la structure JSX ni les props des sous-composants
- `Hero.tsx` et `Introduction.tsx` sont inutilisés (aucune route) — ne pas toucher

</boundaries>

<verification>
- [ ] `npm run build` sans erreur TypeScript
- [ ] `npm run dev` → site visuellement identique à avant (fallbacks)
- [ ] /disciplines affiche les cartes depuis useDisciplines() + sous-titre dynamique
- [ ] /locations affiche les salles depuis useVenues() + sous-titre dynamique
- [ ] /contact affiche les coordonnées depuis useSiteInfo() + sous-titre dynamique
- [ ] /planning : "SAISON {siteInfo.season}" dynamique + sous-titre dynamique
- [ ] /instructors : sous-titre dynamique depuis pageTexts
- [ ] /pricing : sous-titre dynamique depuis pageTexts
- [ ] RegistrationInfo : permanences, docs, photo, téléphone depuis hooks
- [ ] Footer affiche siteInfo.footerTagline
- [ ] AsmeIndex hero tagline utilise content.heroTagline avec fallback
- [ ] Boutons sociaux AsmeIndex conditionnels (masqués si URL vide)
- [ ] Les images Unsplash restent en fallback quand les champs Sanity sont vides
- [ ] Checkpoint human-verify approuvé
</verification>

<success_criteria>
- Tous les tasks complétés
- TypeScript compile sans erreur
- Site visuellement identique aux fallbacks
- Checkpoint approuvé
- Zéro donnée métier hardcodée dans les 7 fichiers modifiés
</success_criteria>

<output>
Après complétion, créer `.paul/phases/04-full-cms/04-03-SUMMARY.md`
</output>
