---
phase: 04-full-cms
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sanity/schemaTypes/siteInfo.ts
  - sanity/schemaTypes/discipline.ts
  - sanity/schemaTypes/venue.ts
  - sanity/schemaTypes/registrationInfo.ts
  - sanity/schemaTypes/pageTexts.ts
  - sanity/schemaTypes/homepage.ts
  - sanity/schemaTypes/index.ts
  - sanity.config.ts
autonomous: true
---

<objective>
## Goal
Créer 3 nouveaux schemas Sanity (siteInfo, discipline, venue) et enrichir le schema homepage avec les champs images manquants + tagline hero.

## Purpose
Rendre 100% du contenu du site éditable depuis le studio Sanity. Sans ces schemas, les disciplines, les salles et les coordonnées restent hardcodés dans le code React.

## Output
6 fichiers modifiés. Le studio Sanity affiche 3 nouvelles sections éditables (Informations du site, Disciplines, Salles) et le schema Page d'accueil expose les champs images.
</objective>

<context>
@.paul/PROJECT.md
@sanity/schemaTypes/homepage.ts
@sanity/schemaTypes/index.ts
@sanity.config.ts
@src/lib/fallbackContent.ts
</context>

<acceptance_criteria>

## AC-1: Schemas enregistrés et visibles dans le studio
```gherkin
Given les 5 nouveaux schemas créés et enregistrés dans index.ts et sanity.config.ts
When on lance `npx sanity dev` et on ouvre le studio
Then les sections "Informations du site", "Disciplines", "Salles", "Informations d'inscription" et "Textes des pages" apparaissent dans la navigation du studio
```

## AC-2: Schema homepage enrichi
```gherkin
Given le schema homepage mis à jour
When on ouvre le document "Page d'accueil" dans le studio
Then 6 nouveaux champs sont présents : Tagline hero, Image Philosophie, Image Mise en avant, Label section mise en avant, Image carte Disciplines, Image carte Cours
```

## AC-3: Aucune régression TypeScript
```gherkin
Given les schemas créés avec defineType/defineField
When on lance `npx tsc --noEmit`
Then aucune erreur TypeScript
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Créer les 3 nouveaux schemas Sanity</name>
  <files>sanity/schemaTypes/siteInfo.ts, sanity/schemaTypes/discipline.ts, sanity/schemaTypes/venue.ts</files>
  <action>
    Créer `sanity/schemaTypes/siteInfo.ts` — singleton (type: 'document'):
    - name: 'siteInfo', title: 'Informations du site'
    - Champs: phone (string, required), email (string, required), mailingAddress (text, rows:2),
      instagramUrl (url), facebookUrl (url), twitterUrl (url, title: 'Lien Twitter / X'),
      season (string, description: 'Ex: 2025–2026'),
      footerTagline (string, description: 'Texte sous le logo en bas de page, ex: Saint-Michel-Chef-Chef · Saison 2025–2026')

    Créer `sanity/schemaTypes/discipline.ts` — liste de documents:
    - name: 'discipline', title: 'Discipline'
    - Champs: title (string, required), iconName (string, options: list ['Zap','Star','Heart','Music'],
      description: 'Icône Lucide à afficher'), description (text, rows:3, required),
      benefits (array of string, title: 'Bénéfices'), order (number, description: 'Ordre d\'affichage')
    - Preview: select title

    Créer `sanity/schemaTypes/venue.ts` — liste de documents:
    - name: 'venue', title: 'Salle'
    - Champs: name (string, required), address (string, required), description (text, rows:3),
      amenities (array of string, title: 'Équipements'), mapEmbedUrl (url, title: 'URL Google Maps Embed'),
      googleMapsUrl (url, title: 'Lien Google Maps direct'), order (number)
    - Preview: select name

    Créer `sanity/schemaTypes/registrationInfo.ts` — singleton (type: 'document'):
    - name: 'registrationInfo', title: 'Informations d\'inscription'
    - Champs:
      permanence1Days (string, title: 'Permanence 1 — Jours', ex: 'Lundis & Mercredis')
      permanence1Hours (string, title: 'Permanence 1 — Horaires', ex: '19h30 – 21h30')
      permanence1Venue (string, title: 'Permanence 1 — Salle')
      permanence2Days (string, title: 'Permanence 2 — Jours')
      permanence2Hours (string, title: 'Permanence 2 — Horaires')
      permanence2Venue (string, title: 'Permanence 2 — Salle')
      requiredDocuments (array of string, title: 'Documents requis pour l\'inscription')
      photoNote (text, rows:2, title: 'Note photo d\'identité')

    Créer `sanity/schemaTypes/pageTexts.ts` — singleton (type: 'document'):
    - name: 'pageTexts', title: 'Textes des pages'
    - Champs (tous string, description précise le contexte):
      planningSubtitle (string, title: 'Sous-titre page Planning')
      disciplinesSubtitle (string, title: 'Sous-titre page Disciplines')
      locationsSubtitle (string, title: 'Sous-titre page Nos Salles')
      contactSubtitle (string, title: 'Sous-titre page Contact')
      instructorsSubtitle (string, title: 'Sous-titre page Professeurs')
      pricingSubtitle (string, title: 'Sous-titre page Tarifs')

    Utiliser defineType/defineField de 'sanity' partout.
  </action>
  <verify>Les 5 fichiers existent et exportent correctement leurs types (pas d'erreur tsc).</verify>
  <done>AC-1 partiellement satisfait: schemas créés, restent à enregistrer.</done>
</task>

<task type="auto">
  <name>Task 2: Mettre à jour homepage.ts, index.ts et sanity.config.ts</name>
  <files>sanity/schemaTypes/homepage.ts, sanity/schemaTypes/index.ts, sanity.config.ts</files>
  <action>
    Dans `homepage.ts`, ajouter 6 champs après le champ `heroImage` existant:
    - heroTagline (string, title: 'Tagline sous le logo (ex: École de Danse · Saint-Michel-Chef-Chef)')
    - philosophyImage (image, hotspot: true, title: 'Image section Philosophie')
    - featuredImage (image, hotspot: true, title: 'Image section mise en avant')
    - featuredSectionLabel (string, title: 'Label section mise en avant (ex: NOTRE APPROCHE)')
    - servicesCard1Image (image, hotspot: true, title: 'Image carte Disciplines')
    - servicesCard2Image (image, hotspot: true, title: 'Image carte Cours & Stages')

    Dans `index.ts`, importer et ajouter siteInfoType, disciplineType, venueType, registrationInfoType, pageTextsType au tableau schemaTypes.

    Dans `sanity.config.ts`, dans la structure du studio, ajouter après le divider existant:
    - listItem 'Informations du site' → document siteInfo, id 'siteInfo' (singleton)
    - listItem 'Disciplines' → documentTypeList 'discipline'
    - listItem 'Salles' → documentTypeList 'venue'
    - listItem 'Informations d\'inscription' → document registrationInfo, id 'registrationInfo' (singleton)
    - listItem 'Textes des pages' → document pageTexts, id 'pageTexts' (singleton)
  </action>
  <verify>
    `npx tsc --noEmit` sans erreur.
    `npx sanity dev` → studio affiche les nouvelles sections dans la nav.
  </verify>
  <done>AC-1, AC-2, AC-3 satisfaits: tous schemas enregistrés, homepage enrichi, TypeScript OK.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- sanity/schemaTypes/instructor.ts
- sanity/schemaTypes/schedule.ts
- sanity/schemaTypes/pricing.ts
- src/ (aucun fichier React dans ce plan)

## SCOPE LIMITS
- Ce plan crée uniquement les schemas Sanity — pas de hooks ni de composants React
- Ne pas modifier les schemas existants (instructor, schedule, pricing)
- Ne pas toucher aux champs homepage existants (seulement ajouter)

</boundaries>

<verification>
- [ ] `npx tsc --noEmit` passe sans erreur
- [ ] Les 5 fichiers schemas créés exportent un type nommé
- [ ] `sanity/schemaTypes/index.ts` importe et exporte les 5 nouveaux types
- [ ] `sanity.config.ts` affiche les 3 nouvelles sections dans la structure du studio
- [ ] `sanity/schemaTypes/homepage.ts` contient les 6 nouveaux champs (dont featuredSectionLabel)
- [ ] Tous les critères d'acceptance vérifiés
</verification>

<success_criteria>
- Tous les tasks complétés
- TypeScript compile sans erreur
- Studio Sanity fonctionnel avec les nouvelles sections visibles
</success_criteria>

<output>
Après complétion, créer `.paul/phases/04-full-cms/04-01-SUMMARY.md`
</output>
