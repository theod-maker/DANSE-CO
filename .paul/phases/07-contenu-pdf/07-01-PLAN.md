---
phase: 07-contenu-pdf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/fallbackContent.ts
  - src/components/layout/AppNavbar.tsx
  - src/components/layout/Footer.tsx
autonomous: true
---

<objective>
## Goal
Ajouter les 4 disciplines manquantes dans fallbackContent.ts, aligner la navbar sur le PDF (labels + routes), corriger les liens du footer, et rendre les réseaux sociaux conditionnels.

## Purpose
Le PDF définit la structure de navigation du site. La navbar actuelle utilise des labels différents (Disciplines, Professeurs, Nos Salles) et le footer pointe vers des routes incorrectes (/profs, /tarifs). Les liens sociaux affichent des # vides.

## Output
- fallbackContent.ts : 4 nouvelles disciplines
- AppNavbar.tsx : labels et routes alignés sur le PDF + ajout L'histoire et Actualités
- Footer.tsx : nav corrigée + réseaux sociaux conditionnels via useSiteInfo
</objective>

<context>
@.paul/PROJECT.md
@src/lib/fallbackContent.ts
@src/components/layout/AppNavbar.tsx
@src/components/layout/Footer.tsx
@src/hooks/useSanity.ts
</context>

<acceptance_criteria>

## AC-1: Disciplines manquantes présentes
```gherkin
Given fallbackDisciplines dans fallbackContent.ts
When on lit le tableau
Then Lindy Hop, Multidanses, Danse en ligne et Cours enfants y figurent avec _id, title, iconName, description et benefits
```

## AC-2: Navbar alignée sur le PDF
```gherkin
Given la navbar du site
When on la lit
Then les liens sont : Planning, Les danses (/disciplines), Les membres (/instructors), Tarifs (/pricing), L'histoire (/histoire), Actualités (/actualites)
And "Nos Salles" n'y figure plus
And le bouton CTA reste "Contact" → /contact
```

## AC-3: Footer nav corrigée
```gherkin
Given le footer du site
When on clique sur les liens de navigation
Then ils pointent vers les bonnes routes : /, /planning, /instructors, /pricing, /contact
```

## AC-4: Réseaux sociaux conditionnels
```gherkin
Given instagramUrl et facebookUrl vides dans fallbackSiteInfo
When le footer s'affiche
Then aucun lien Instagram ni Facebook n'est rendu dans le DOM
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1 : 4 disciplines manquantes dans fallbackDisciplines</name>
  <files>src/lib/fallbackContent.ts</files>
  <action>
    Ajouter à la fin du tableau fallbackDisciplines les 4 entrées suivantes (sans modifier les 4 existantes) :

    { _id: 'lindy-hop', title: 'Lindy Hop', iconName: 'Zap',
      description: "Danse swing née dans les années 1920 à New York. Se danse à deux, caractérisée par son énergie, son improvisation et ses mouvements dynamiques. L'une des danses swing les plus pratiquées dans le monde.",
      benefits: ['Énergie', 'Improvisation', 'Convivialité'] }

    { _id: 'multidanses', title: 'Multidanses', iconName: 'Music',
      description: "Explorez plusieurs styles de danse : chachacha, tango, rock'n roll, salsa. Chaque rythme a sa magie et permet de découvrir de nouvelles techniques tout en s'amusant.",
      benefits: ['Variété', 'Découverte', 'Technique'] }

    { _id: 'danse-en-ligne', title: 'Danse en ligne', iconName: 'Users',
      description: "Danse collective où tout le monde suit une même chorégraphie. Facile à apprendre et très conviviale, se danse sur tous les styles de musique : rumba, shim sham, chachacha, salsa.",
      benefits: ['Collectif', 'Accessible', 'Convivialité'] }

    { _id: 'cours-enfants', title: 'Cours enfants', iconName: 'Star',
      description: "Nos petits danseurs découvrent le rythme, la coordination et la créativité à travers les danses latines : samba, chachacha, rumba, paso doble, jive. Une façon ludique de bouger, s'amuser et prendre confiance dès le plus jeune âge.",
      benefits: ['Rythme', 'Créativité', 'Confiance en soi'] }
  </action>
  <verify>fallbackDisciplines contient 8 entrées.</verify>
  <done>AC-1 satisfait.</done>
</task>

<task type="auto">
  <name>Task 2 : Navbar alignée sur le PDF</name>
  <files>src/components/layout/AppNavbar.tsx</files>
  <action>
    Remplacer le tableau navLinks par :

    const navLinks = [
      { label: 'Planning', href: '/planning' },
      { label: 'Les danses', href: '/disciplines' },
      { label: 'Les membres', href: '/instructors' },
      { label: 'Tarifs', href: '/pricing' },
      { label: "L'histoire", href: '/histoire' },
      { label: 'Actualités', href: '/actualites' },
    ]

    Supprimer l'entrée 'Nos Salles'. Le bouton CTA "Contact" → /contact reste inchangé.
    Ne modifier que le tableau navLinks, pas la structure JSX ni les styles.
  </action>
  <verify>La navbar affiche les 6 liens dans l'ordre du PDF, sans "Nos Salles".</verify>
  <done>AC-2 satisfait.</done>
</task>

<task type="auto">
  <name>Task 3 : Footer nav + réseaux sociaux conditionnels</name>
  <files>src/components/layout/Footer.tsx</files>
  <action>
    1. Corriger les liens de navigation dans la section <div className={styles.links}> :
       - Accueil → /
       - Planning → /planning
       - Les membres → /instructors  (était /profs)
       - Tarifs → /pricing  (était /tarifs)
       - L'histoire → /histoire  (nouveau)
       - Actualités → /actualites  (nouveau)
       - Contact → /contact
       Supprimer le lien "Les Professeurs" s'il existe en doublon.

    2. Réseaux sociaux conditionnels :
       - Importer useSiteInfo depuis '../../hooks/useSanity'
       - Dans Footer, appeler : const { instagramUrl, facebookUrl } = useSiteInfo()
       - Remplacer le bloc <div className={styles.socials}> par un rendu conditionnel :
         * Rendre le div seulement si instagramUrl || facebookUrl est truthy
         * Chaque <a> seulement si son URL est non-vide
         * href = l'URL, target="_blank", rel="noopener noreferrer"
       - Conserver InstagramIcon et FacebookIcon existants
  </action>
  <verify>
    1. Les liens du footer pointent vers les bonnes routes.
    2. Avec instagramUrl vide dans fallbackSiteInfo : le bloc socials n'est pas dans le DOM.
  </verify>
  <done>AC-3 et AC-4 satisfaits.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Les 4 disciplines existantes dans fallbackDisciplines
- La structure JSX et les styles de AppNavbar.tsx (seulement navLinks)
- Le CSS Footer.module.css
- fallbackSiteInfo (ne pas ajouter de vraies URLs ici)

## SCOPE LIMITS
- Ne pas créer les pages /histoire et /actualites ici (plan 07-02)
- Ne pas toucher aux schemas Sanity

</boundaries>

<verification>
- [ ] fallbackDisciplines contient 8 entrées
- [ ] Navbar : 6 liens dans l'ordre correct, pas de "Nos Salles"
- [ ] Footer : routes corrigées, socials conditionnels
- [ ] npx tsc --noEmit sans erreur
</verification>

<success_criteria>
- Toutes les tâches complétées
- TypeScript sans erreur
- Aucune régression sur le reste du site
</success_criteria>

<output>
Après completion, créer `.paul/phases/07-contenu-pdf/07-01-SUMMARY.md`
</output>
